import * as auth from './api/auth';
import * as projects from './api/projects';
import * as updatePackages from './api/update-packages';

export default {
   api: { auth, projects, updatePackages },
};
